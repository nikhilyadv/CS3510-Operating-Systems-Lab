int main()
{
decl
    str r;
    int temp,fd;
enddecl
begin
    fd=exposcall("Open","root");
    temp=exposcall("Read",fd, r);
    while (r != "-1") do
		temp=exposcall("Write",-2,r);
		temp=exposcall("Seek",fd,7);
		temp=exposcall("Read",fd, r);
	endwhile;
    return 0;
end
}
